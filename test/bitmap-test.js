'use strict';

var expect = require('chai').expect;
var readColors = require(__dirname + '/../lib/read-colors.js');
var readData = require(__dirname + '/../lib/read-data.js');

describe('testing Color Inverse function', () => {
  it('the test I need to write/figure out', () => {
    expect(colors.writeColors(i, bitmap, invertedColorsObj)).to.eql(' ');
  });
});
